#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, m;
    cin >> n >> m;
    vector<int> arr(n + 1);

    for (int i = 1; i < n + 1; i++)
    {
        cin >> arr[i];
    }

    sort
    while (true)
    {
        int mid = n / 2;
        if (arr[n] )
    }
    return 0;
}